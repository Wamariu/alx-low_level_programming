#include "main.h"
/**
 * positive_or_negative - tests function that prints if integer is +ve or -ve
 * @i: integer being tested
 * Return: 0
 */
void positive_or_negative(int i)
{
	int i;

	i = 98;
	positive_or_negative(i);

	return (0);
}
