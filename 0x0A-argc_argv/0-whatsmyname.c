#include "main.h"
#include <stdio.h>
/**
 * main - print the name of program
 * @argc: arguement count
 * @argv: array of strings
 * Return: 0
 */
int main(int argc, char *argv[])
{
	(void) argc;

	printf("%s\n", argv[0]);

	return (0);
}
