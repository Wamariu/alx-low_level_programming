#include "main.h"
/**
 * _abs - computes absolute values of an integer
 * @int: integer
 *
 * Return: The absolute value of the integer
 */
int _abs(int);
{
	int x;

	if (x >= 0)
		return (n);
	else
		return (-n);
}
