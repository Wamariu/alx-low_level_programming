#include "main.h"
/**
 * main - prints alphabet in lowercase followed by newline
 *
 * Return: 0
 */
void print_aphabet(void);
{
	char c;

	for (c = 'a'; c <= 'z'; c++)
		_putchar(c);

	_putchar('\n')
}
